# Hooks & Filters

> **Status:** stub — outline only. Grep `do_action(` / `apply_filters(` across `includes/` to enumerate.

The extensibility surface — how integrations and especially `wp-scheduled-posts-pro` hook in.

## Actions
_TODO — table of key actions, when they fire, args._

## Filters
_TODO — table of key filters, what they modify, args._

## How Pro hooks in
_TODO — Pro features live in the separate `wp-scheduled-posts-pro` plugin and integrate via these hooks + shared subsystems. Don't add Pro-only logic to the free repo._

## Source
- Grep `includes/` for `do_action(` and `apply_filters(`.
- [../../CLAUDE.md](../../CLAUDE.md)
