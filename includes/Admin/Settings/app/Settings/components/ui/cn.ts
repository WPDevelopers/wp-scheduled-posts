import classNames from 'classnames';

/**
 * Shorthand for the conditional-class pattern every component here uses.
 * Kept as its own module so a future swap to `tailwind-merge` touches one file.
 */
export const cn = classNames;

export default cn;
